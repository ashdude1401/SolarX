import 'package:flutter/material.dart';

class ConnectWithExpert extends StatelessWidget {
  const ConnectWithExpert({super.key});

  static const String routeName = "connect-with-user";

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Connect With Expert")),
    );
  }
}
